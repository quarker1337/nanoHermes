# Notes

Historical notes, design scraps, and non-entrypoint markdown live here instead of the repository root.

Keep durable maintainer documentation in the most specific docs folder available. Use this directory only when a note does not fit `docs/nanohermes/`, `docs/plans/`, `docs/security/`, or `docs/releases/`.
