# Examples

Curated runnable examples live here.

Rules:

- Keep examples small and reviewable.
- Put generated output under `examples/generated/` or `examples/local/`; both are ignored by git.
- Prefer examples that can be run from the repository root without modifying real Hermes user state.
